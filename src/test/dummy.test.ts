// TODO Remove me after first real unit test is present
describe("it", () => {
    it("just works", () => {
        const div = document.createElement("div");
        expect(div).toBeInstanceOf(HTMLDivElement);
    });
});
